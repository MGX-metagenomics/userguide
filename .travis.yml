notifications:
  email:
    recipients:
      - sebastian@jaenicke.org

language: bash

dist: trusty

branches:
  only:
    - master

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y texlive-latex-base texlive-fonts-recommended texlive-latex-extra texlive-fonts-extra texlive-latex-recommended

script:
  - make

